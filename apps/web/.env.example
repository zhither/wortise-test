# Vacío en desarrollo: Vite hace proxy de /trpc y /api al API (puerto 3000).
# Si el front y el API están en dominios distintos (p. ej. Vercel + Railway), apuntá al backend:
# VITE_API_URL=https://tu-api.railway.app
VITE_API_URL=
