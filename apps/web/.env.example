# Vacío: en dev Vite proxifica /trpc y /api al :3000; en Vercel conviene igual (vacío) + rewrites
# en vercel.json hacia tu API para que la cookie de sesión sea mismo origen (móvil / incógnito).
# Si ponés la URL del API aquí, el front llama a otro dominio y el login puede fallar por cookies.
# VITE_API_URL=https://tu-api.vercel.app
VITE_API_URL=
